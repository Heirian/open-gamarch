<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= button_tag type: 'submit', class: "btn btn-purple", title: t(:get_after_explanation) do %>
    <i class="fa fa-crosshairs" aria-hidden="true" title="<%= t(:follow) %>"></i> <%= t(:Get_after) %>
  <% end %>
<% end %>
