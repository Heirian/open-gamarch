<% provide(:title, t(:signup)) %>

<%= render 'shared/forms_title', page_title: t(:gaming_better), page_subtitle: t(:improve_community) %>

<%= render 'shared/errors', object: @user %>

<%= render 'form', object: @user %>
