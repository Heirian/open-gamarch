<div class="container">
  <div class="col-12">
    <div class="col-md-4 col-md-offset-4 col-sm-12 col-xs-12">
      <div class="center">
        <h1><%= @game.name %></h1>
        <p class="justify"><%= @game.description %></p>
        <p><%= render @game.developers %></p>
        <p><%= @game.release %></p>
        <a href="<%= @game.website %>" target="_blank">Website</a>
      </div>
    </div>
  </div>

</div>
