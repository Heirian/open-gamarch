<%= render 'shared/page_title', title: @article.name %>

<div class="row">
  <div class="well justify col-xs-10 col-xs-offset-1 col-sm-10 col-sm-offset-1 col-md-10 col-md-offset-1">
    <%= simple_format(@article.description) %>
    <hr />
    <div class="pull-right">
      <small>
        <%= link_to gravatar_for(@article.user), user_path(@article.user) %>
        <div class="separator">
          Created by: <%= @article.user.name.capitalize %>
        </div>
        Created <%= time_ago_in_words(@article.created_at) %> ago
      </small>
    </div>
    <div class="pull-left well-footer">
      <%= link_to "Return to articles listing", articles_path, class: "btn btn-purple" %>
      <% if logged_in? %>
        <%= link_to "Create a new article", new_article_path, class: "btn btn-purple" %>
      <% end %>
      <% if logged_in? && (current_user == @article.user || current_user.admin?) %>
        <%= link_to "Edit this article", edit_article_path(@article), class: "btn btn-purple" %>
        <%= link_to "Delete this article", article_path(@article), method: :delete,
                                                                data: { confirm: "Are you sure?" },
                                                                class: "btn btn-purple" %>
      <% end %>
    </div>
  </div>
</div>

<hr />

<% if logged_in? %>
  <div class="row">
    <div class="col-md-12">
      <%= form_for([@article, @comment], :html => { class: "form-horizontal", role: "form" }) do |f| %>
        <div class="form-group">
          <div class="col-md-8 col-md-offset-1">
            <%= f.text_field :description, class: "form-control", placeholder: "Write a comment...", autofocus: true %>
          </div>
          <div class="col-md-2">
            <%= f.submit "Post", class: "btn btn-purple" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
<% else %>
  <h4 class="center">Have a account? <%= link_to "Log in to comment", login_path %></h4>
  <h4 class="center">Are you new here? <%= link_to "Sign in to comment", signup_path %></h4>
<% end %>

<hr />

<% if @article.comments.any? %>
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <h3>
        Prior comments:
      </h3>
      <div id="messages">
        <%= render partial: 'comments/comments', object: @comments %>
      </div>
    </div>
  </div>
<% else %>
  <h3 class="center">No comments yet</h3>
<% end %>
